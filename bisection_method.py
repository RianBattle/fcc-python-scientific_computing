def square_root_bisection(square_target, tolerance=1e-7, max_iterations=100):
    """
    square_target: number for which to find the square root
    tolerance: acceptable difference between the square of the approximate root and the actual target value
    max_iterations: the max # of iterations to perform
    """
    pass
